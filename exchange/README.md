# exchange

The deployable exchange application — a thin shell around [cembalo](../cembalo/README.md), which contains all the actual logic. There is no code here: the application is assembled from Spring wiring and configuration, and launched through `continuo.Main`.
