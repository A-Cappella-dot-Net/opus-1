# dev-tools

The deployable web development tool that includes a Publisher and a Subscriber. The Publisher allows a developer to publish messages, while the Subscriber allows the developer to view live messages from a browser. Assembled from Spring wiring and launched through `continuo.Main`.
